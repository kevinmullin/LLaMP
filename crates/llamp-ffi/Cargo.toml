[package]
name = "llamp-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
llamp-audio = { path = "../llamp-audio" }
llamp-core = { path = "../llamp-core" }
llamp-plugin-api = { path = "../llamp-plugin-api" }
llamp-plugin-host = { path = "../llamp-plugin-host" }
llamp-skin = { path = "../llamp-skin" }
llamp-source-local = { path = "../../plugins/llamp-source-local" }

[dev-dependencies]
xtask = { path = "../../xtask" }
