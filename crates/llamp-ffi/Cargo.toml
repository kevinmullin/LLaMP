[package]
name = "llamp-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
llamp-audio = { path = "../llamp-audio" }
llamp-core = { path = "../llamp-core" }
llamp-library = { path = "../llamp-library" }
llamp-skin = { path = "../llamp-skin" }

[dev-dependencies]
xtask = { path = "../../xtask" }
